# OpenHIM Core API server config
server {
    listen                80;
    server_name           openhimcomms.*;

    location /.well-known/acme-challenge/ {
        resolver          127.0.0.11 valid=30s;
        set               $upstream_certbot certbot;
        proxy_pass        http://$upstream_certbot$request_uri;
    }

    location / {
        return            301 https://$host$request_uri;
    }
}
server {
    listen                443 ssl;
    listen                [::]:443 ssl;
    server_name           openhimcomms.*;

    location /.well-known/acme-challenge/ {
        resolver          127.0.0.11 valid=30s;
        set               $upstream_certbot certbot;
        proxy_pass        http://$upstream_certbot$request_uri;
    }

    location / {
        resolver          127.0.0.11 valid=30s;
        set               $upstream_openhim_core openhim-core;
        proxy_pass        https://$upstream_openhim_core:8080;
    }
}

# OpenHIM Core HTTP server config
server {
    listen                80;
    server_name           openhimcore.*;

    location /.well-known/acme-challenge/ {
        resolver          127.0.0.11 valid=30s;
        set               $upstream_certbot certbot;
        proxy_pass        http://$upstream_certbot$request_uri;
    }

    location / {
        return            301 https://$host$request_uri;
    }
}
server {
    listen                443 ssl;
    listen                [::]:443 ssl;
    server_name           openhimcore.*;
    client_max_body_size  10M;

    # Handle 502 errors (unavailable services) by returning 404
    proxy_intercept_errors on;
    error_page 502 =404 /502-error;
    
    location = /502-error {
        internal;
        return 444;
    }

    location /.well-known/acme-challenge/ {
        resolver          127.0.0.11 valid=30s;
        set               $upstream_certbot certbot;
        proxy_pass        http://$upstream_certbot$request_uri;
    }

    location / {
        resolver          127.0.0.11 valid=30s;
        set               $upstream_openhim_core openhim-core;
        proxy_pass        https://$upstream_openhim_core:5000;
    }
}

# OpenHIM Console
server {
    listen                80;
    server_name           openhimconsole.*;

    location /.well-known/acme-challenge/ {
        resolver          127.0.0.11 valid=30s;
        set               $upstream_certbot certbot;
        proxy_pass        http://$upstream_certbot$request_uri;
    }

    location / {
        return            301 https://$host$request_uri;
    }
}
server {
    listen                443 ssl;
    listen                [::]:443 ssl;
    server_name           openhimconsole.*;

    location /.well-known/acme-challenge/ {
        resolver          127.0.0.11 valid=30s;
        set               $upstream_certbot certbot;
        proxy_pass        http://$upstream_certbot$request_uri;
    }

    # The console reads the core's hostname from here on every load. Served without a
    # Cache-Control header a browser caches it heuristically, so after a domain change the
    # console keeps talking to the old host until the user clears site data.
    location /config/ {
        resolver          127.0.0.11 valid=30s;
        set               $upstream_openhim_console openhim-console;
        proxy_pass        http://$upstream_openhim_console:80;
        add_header        Cache-Control "no-store" always;
    }

    location / {
        resolver          127.0.0.11 valid=30s;
        set               $upstream_openhim_console openhim-console;
        proxy_pass        http://$upstream_openhim_console:80;
    }
}
